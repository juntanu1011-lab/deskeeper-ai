# dependencies
node_modules/

# next.js
.next/
out/
build/

# npm cache — the global ~/.npm is root-owned and broken on this machine, so
# installs run with `--cache ./web/.npm-cache`. Never commit it.
.npm-cache/

# env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# vercel
.vercel

# typescript
*.tsbuildinfo

# macOS
.DS_Store

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# CV smoke test: personal photos & binaries — never commit
lab/cv-smoke/IMG_*
lab/cv-smoke/facetest

# Mascot art: unprocessed generator output (27MB) and the pre-2D versions we
# replaced. Kept on disk, not in git — `superseded/` is recoverable from git
# history, `raw/` is reproducible from design/mascot-2d-trials/prompts.md.
design/mascot-2d-trials/raw/
design/mascot-2d-trials/superseded/

# Scratch output from asset review passes
.mascot-check/
